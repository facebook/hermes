#!/usr/bin/env python

import os
import sys

# Make sure we can find the lit package.
sys.path.insert(0, os.path.join("@LLVH_SOURCE_DIR@", 'utils', 'lit'))

if __name__=='__main__':
    from lit.main import main
    main({})
