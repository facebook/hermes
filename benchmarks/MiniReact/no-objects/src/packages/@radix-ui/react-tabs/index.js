/**
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 *
 * @flow strict-local
 * @format
 */

import type {Props, React$MixedElement} from 'react';

// TODO: https://github.com/radix-ui/primitives/blob/main/packages/react/tabs/src/Tabs.tsx

export function List(props: Props): React$MixedElement {
  return <div {...props} />;
}

export function Trigger(props: Props): React$MixedElement {
  return <div {...props} />;
}

export function Content(props: Props): React$MixedElement {
  return <div {...props} />;
}

export function Tabs(props: Props): React$MixedElement {
  return <div {...props} />;
}

export const Root = Tabs;
